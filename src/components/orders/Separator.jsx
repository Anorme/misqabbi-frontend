// A simple visual separator used under headings or between sections
const Separator = () => (
  <div className="w-full flex items-center justify-center py-2">
    <div className="h-0.5 w-24 bg-msq-purple-light rounded" />
  </div>
);

export default Separator;
